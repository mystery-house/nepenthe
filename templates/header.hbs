\version "{{version}}"

% Preprocessed by Pitcher Plant

\header {
{{#if dedication }}
  dedication = "{{dedication}}"
{{/if ~}}
{{~#if title }}
  title = "{{title}}"
{{/if ~}}
{{~#if subtitle }}
  subtitle = "{{subtitle}}"
{{/if ~}}
{{~#if subsubtitle }}
  subsubtitle = "{{subsubtitle}}"
{{/if ~}}
{{~#if instrument }}
  instrument = "{{instrument}}"
{{/if ~}}
{{~#if poet}}
  poet = "{{poet}}"
{{/if}}
{{~#if composer}}
  composer = "{{composer}}"
{{/if ~}}
{{~#if meter}}
  meter = "{{meter}}"
{{/if ~}}
{{~#if arranger}}
  arranger = "{{arranger}}"
{{/if ~}}
{{~#if tagline}}
  tagline = "{{tagline}}"
{{~else}}
  tagline = "Pre-processed with PitcherPlant"
{{/if ~}}
{{~#if copyright}}
  copyright = "{{copyright}}"
{{/if ~}}
}

{{!-- This tag is used by the Nepenthe `staff` tag to --}}
{{!-- format staves as Lilypond markup.               --}}

        \new {{{ staffType }}} \with {
            {{#if instrumentName}}instrumentName = #"{{{instrumentName}}}"{{/if}}
            {{#if shortInstrumentName}}shortInstrumentName = #"{{{shortInstrumentName}}}"{{/if}}
        } {
            {{#if clef}}\clef "{{{clef}}}"{{/if}}
            \\{{{ staffPart }}}Part
        }
